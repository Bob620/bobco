/*
document.addEventListener("DOMContentLoaded", function() {
  for (var i = 0; i < document.getElementById('nav').children.length; i++) {
    var child = document.getElementById('nav').children[i];
    if (child.tagName === 'LABEL') {
      child.addEventListener('click', function(element) {
        window.history.pushState({}, '', element.currentTarget.htmlFor);
      });
    }
  }
});
*/
